<h1>E-Commerce App</h1>

<% if @warehouses.empty? %>
  <p>Nenhum galpão encontrado</p>
<% end %>

<% @warehouses.each do |w| %>
  <div>
    <%= link_to w["name"], warehouse_path(w["id"]) %><br />
    <%= w["code"]%>
  </div>
  <br />
<% end %>